import cat

# Calls each function without arguments
cat.purr()
cat.lick()
cat.nap()

# Calls each function again with Kitty arguments
cat.purr( 'Kitty' )
cat.lick( 'Kitty' )
cat.nap( 'Kitty' )